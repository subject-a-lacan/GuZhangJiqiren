# 项目报告需要用到的信息（按报告章节组织）

---

## 一、题目要求

### 1.1 基本要求（1）
- AB发车点发车，逆时针行驶1圈后停在发车点
- 启动和停车有声光提示
- D1 ≤ 3cm（停车时前沿中心点与AD线外沿距离）
- t1 ≤ 15s

### 1.2 基本要求（2）
- AB发车点或AD发车点（裁判选择），3/4圈后掉头原路返回
- BC边覆盖干扰A4纸（3种类型，裁判任选）
- AB发车 → 逆时针至B点掉头返回；AD发车 → 顺时针至D点掉头返回
- D2 ≤ 3cm，t2 ≤ 25s

### 1.3 发挥部分（1）
- 载330ml矿泉水，AB或AD发车
- CD边中部横向贴放待测A4纸，测l1和l2（1cm ≤ l1,l2 ≤ 5cm）
- 无线传输l1,l2至手机显示，相对误差 ≤ 20%
- 沿轨迹行驶1圈后停在发车点
- D3 ≤ 3cm，t3 ≤ 15s

### 1.4 发挥部分（2）
- 载330ml矿泉水，AB发车
- CD边贴放待测A4纸，判断形状类型（圆形/正方形/三角形/椭圆形）
- 4种形状分别放置在对角线AC和BD的三等分点（共4点）
- 停车时小车外轮廓压上对应图形
- t4 ≤ 30s

### 场地参数
- 正方形外边长 100cm × 100cm
- 黑色胶带宽 1.8cm ± 0.2cm
- 小车尺寸 ≤ 20cm × 15cm × 25cm
- 轮式小车，轮数3-4个，不得采用履带和麦氏轮

---

## 二、系统方案细节

### 2.1 硬件清单
| 模块 | 型号 | 接口 |
|---|---|---|
| 主控 | STM32G431KBU6 | - |
| 灰度传感器 | 8路模拟灰度（淘宝链接见gw_anagloge.h） | ADC3 + CD4051模拟开关 |
| 陀螺仪 | GY901 (JY901S) | I2C1，地址0xA1 |
| 电机驱动 | TB6612 | TIM8 PWM + GPIO方向 |
| 编码器 | 13线霍尔编码器，减速比1:28 | TIM1/TIM2正交解码 |
| 视觉 | MaixCam (K230) | USART3 |
| 无线 | 蓝牙/ESP8266 | USART2/UART4 |
| 舵机 | 2路 | TIM15 PWM |

### 2.2 UART分配
| UART | 用途 | 波特率 |
|---|---|---|
| USART1 | 日志/PID调参/PC通信 | 115200 |
| USART2 | printf输出 / 手机蓝牙 | 115200 |
| USART3 | MaixCam K230通信 | 115200 |
| UART4 | VOFA+/备用 | 115200 |

### 2.3 定时器分配
| 定时器 | 用途 |
|---|---|
| TIM1 | 编码器1（左轮） |
| TIM2 | 编码器2（右轮） |
| TIM3 | 编码器3 |
| TIM4 | 编码器4 |
| TIM5 | 1ms系统时钟中断 |
| TIM8 | 4路PWM（电机） |
| TIM15 | 舵机PWM |

---

## 三、控制方法与理论（代码证据）

### 3.1 灰度循迹

**文件**：`User/Sensor/gw_analogue.c`, `User/Status/status.c`

**传感器读取链**（driver_gw_analogue, line 448-453）：
```
get_gw_raw_data()           → channel[0..7] 原始ADC值 (0-4095)
get_gw_analoge_digital_data() → digital_8bit 迟滞比较器二值化
get_gw_analogue_analogue_diff() → diff 中间4路加权偏差
get_road_type()              → cross.cross 路口类型观测
```

**迟滞比较器**（get_gw_analoge_digital_data, line 234-242）：
```c
// 高阈值/低阈值双阈值迟滞，防抖动
if (channel[i] > digital_high_threshold[i])  → 白，bit清零
else if (channel[i] < digital_low_threshold[i]) → 黑，bit置1
else  → 保持上一次的值（迟滞带）
```

**加权偏差计算**（get_gw_analogue_analogue_diff, line 282-295）：
- 中间4路（通道2-5，对应传感器物理位置-15/-10/10/15）
- 反归一化：buff[i] = 100 - normalize(channel[i], correction_data_w[i], correction_data_b[i])
- 4路权重归一化（权重和=1）
- diff = Σ(buff[i] × distance[i])
- 负值=线偏左，正值=线偏右

**校准函数**（correct_gw_analogue, line 171-221）：
- 白校准时存储correction_data_w[]
- 黑校准时存储correction_data_b[]
- 自动计算阈值：低阈值 = 黑值 + (白值-黑值)×0.33；高阈值 = 黑值 + (白值-黑值)×0.66

**循迹PID差速**（follow_line, status.c line 233-237）：
```c
float diff = compute_pid(&follow_line_pid, gw_analogue.diff);
wheel[0].tar_speed = base_speed - (int16_t)diff;  // 左轮
wheel[1].tar_speed = base_speed + (int16_t)diff;  // 右轮
```
空载巡线PID：kp=1.0, ki=0.03, kd=0, T=20

**路口识别状态机**（get_road_type, line 389-435）：
- 正常状态(Straight)：外侧传感器(bit7或bit0)看到线 → 启动maybe计数器(=integral_times=5)
- 5帧内持续累积integral = integral | digital_8bit
- maybe减到1时：road_decision()根据累积的integral判定路口类型
- 非Straight状态：外侧无横线(bit7/bit0=0)且中间有线 → 恢复Straight

**路口编码**（road_new_from_bit, line 303-309）：
```c
CrossRoad=0b111, TBRoad=0b101, TLRoad=0b011, TRRoad=0b110,
LeftRoad=0b001, RightRoad=0b100, Straight=0b010, UnknowRoad=0b000
```

### 3.2 前馈控制

**文件**：`User/Motor/wheel.c`, `qiankui.md`

**原始标定数据**（qiankui.md）：
| PWM CCR | 平均速度 (pulse/20ms) | 样本数 |
|:-------:|:---:|:---:|
| 500 | 18.6 | 23 |
| 600 | 24.2 | 69 |
| 700 | 29.8 | 31 |
| 800 | 34.9 | 40 |
| 900 | 41.2 | 42 |
| 1000 | 46.4 | 29 |
| 1160 | 54.4 | 35 |

**线性回归结果**（qiankui.md）：
- 启动阈值（静摩擦）：253 CCR（实测253不动，≥254才转）
- 稳态截距（动摩擦）：157 CCR（线性回到speed=0的截距）
- 前馈斜率：18.3 CCR/(pulse/20ms)
- 平均绝对误差：4.9 CCR
- R² = 0.999

**前馈公式**（driver_wheel, line 129-136）：
```c
float ff_abs = wheel_ff_offset[ff_index] + wheel_ff_k[ff_index] * ABS(wheel->tar_speed);
if (ff_abs < wheel_ff_min[ff_index]) ff_abs = wheel_ff_min[ff_index];  // 低于死区抬到最小值
float ff = (wheel->tar_speed > 0) ? ff_abs : ((wheel->tar_speed < 0) ? -ff_abs : 0);
```

**最终PWM输出**：
```c
float pid_out = compute_pid(&wheel->wheel_pid, tar_speed - cur_speed);
wheel->trust = (int16_t)(ff + pid_out);
wheel->trust = CONFINE(wheel->trust, -3000, 3000);  // 限幅
```

**速度-PWM对照表**（qiankui.md，空载参数）：
| 目标速度 | 前馈PWM CCR | 占空比 |
|:---:|:---:|:---:|
| 0 | 0 | 0% |
| 10 | 340 | 11.3% |
| 20 | 523 | 17.4% |
| 30 | 706 | 23.5% |
| 40 | 889 | 29.6% |
| 50 | 1072 | 35.7% |
| 60 | 1255 | 41.8% |

**空载/负重两套参数**：
- BASIC（空载）：offset=114.70/97.26, k=18.28/17.80, min=220
- ADV（负重）：offset=124.64/164.53, k=19.23/17.64, min=260

### 3.3 角度环与转弯

**文件**：`User/Status/status.c`

**角度误差计算**：
```c
float target = tar_angle + initial_angle;  // 绝对角度
float diff = target - cur_angle;
if (diff > 180) diff -= 360;       // 归一化到±180°
else if (diff < -180) diff += 360;
```

**角度环PID输出**（keep_angle, status.c line 239-252）：
```c
float diff = compute_pid(&keep_angle_pid, diff_angle);
diff = CONFINE(diff, -angle_output_limit, angle_output_limit);  // 限幅
wheel[0].tar_speed = base_speed + diff;   // 左轮
wheel[1].tar_speed = base_speed - diff;   // 右轮
```

**转弯参数（Defect.c各task可独立配置）**：
- TASK1：A点80°，D/C/B点85°，角度容差8°，角度限幅20-22
- TASK2：90°转弯约76-86°，掉头175°/-170°，限幅27-93
- TASK3：转弯83-85°，容差10°

**转弯流程标准化**（以TASK3为例）：
1. 检测到路口 → 记录initial_angle = cur_angle，设置tar_angle
2. motion = KEEP_ANGLE，base_speed = 转弯速度
3. angle_error < 容差阈值 → 转弯完成
4. motion = FIND_LINE，base_speed = 低速找线
5. middle6_seen() → 找到线 → SIDE阶段正常循迹

### 3.4 编码器测距

**文件**：`User/Tool/pid.h`, `User/Tool/pid.c`

**编码器参数**（pid.h line 28-33）：
```c
#define ENCODER_PPR 13           // 编码器线数
#define GEAR_RATIO 28.0f         // 减速比1:28
#define WHEEL_DIAMETER_CM 6.723f // 轮径
#define WHEEL_CIRCUMFERENCE_CM (π × 6.723)
// 四倍频后：PULSES_PER_WHEEL_REV = 13 × 4 × 28 = 1456
// CM_PER_PULSE = π×6.723/1456 ≈ 0.0145 cm/pulse
```

**速度读取**（get_wheel_speed, wheel.c line 45-61）：
```c
// 20ms一次，读取CNT - 30000（人为零点偏移，防负数溢出），然后CNT=30000
speed = TIMx->CNT - 30000;
TIMx->CNT = 30000;
return speed * wheel->dir;  // 单位：pulse/20ms
```

**里程转换**（encoder_pulse_to_cm, pid.c line 64-66）：
```c
float encoder_pulse_to_cm(int32_t pulse) {
  return (float)pulse * CM_PER_PULSE;
}
```

**里程累加**（update_task, Defect.c line 1562-1566）：
```c
// 每20ms取左右轮编码器脉冲绝对值求平均累加
status->task.phase_mileage += (|wheel0_speed| + |wheel1_speed|) / 2.0f;
```

**TASK3 CD边测距逻辑（main.c）**：
- K230视觉模块测出l1/l2 → UART发"Cl<值>\r\n"和"CL<值>\r\n"
- STM32 main.c UART调参解析：case 'l': l1 = val; case 'L': l2 = val;
- task3完成后printf输出：printf("%.2f,%.2f\r\n", l1, l2);

### 3.5 陀螺仪

**文件**：`User/Sensor/gy901.c`, `User/Sensor/gy901.h`

- 型号：GY901 (JY901S)
- I2C地址：0xA1
- 数据起始地址：0x34（加速度/角速度/欧拉角共24字节）
- yaw角：寄存器0x3F（持续读取，无漂移归零问题）
- 数据换算：yaw = raw × 180 / 32768（范围±180°）
- 初始化后延迟50ms读取initial_angle（after_init_state）

### 3.6 干扰A4纸处理（TASK2）

**三种干扰类型**（TASK/tupianjieshi.md）：
1. 左伸出横条（7.5cm）：灰度重心左偏
2. 右伸出横条（7.5cm）：灰度重心右偏
3. 长横条贯穿（16.8cm）：类似假十字路口，接近全黑

**处理方法**：
- BC/CB边里程门控：Q2_BC_ROAD_ENABLE_CM = 79cm，前79cm禁止消费路口
- T路口映射：TLRoad→LeftRoad, TRRoad→RightRoad（task2/3_map_road）
- 过干扰区降速、削弱循迹修正

### 3.7 按键逻辑

**文件**：`User/Device/button.c`

| 按键 | 事件 | 条件 | 操作 |
|---|---|---|---|
| PB11 | 短按(UP) | task_running==0 | 轮换task_id 1→2→3→4→1 |
| PB11 | 长按 | TASK2或TASK3空闲 | 切换AB/AD + 蜂鸣器长响1000ms |
| PD2 | 短按(UP) | armed==0且task_running==0 | 发车请求start_request=1 + 蜂鸣器短响200ms |
| PD2 | 长按 | 任意 | 灰度校准correct_gw_analogue() |

**长按/短按互斥**：通过long_triggered标志，长按触发后短按被抑制。
**长按时间**：LONG_PRESS_CNT × 20ms（当前约400ms）。

### 3.8 LED状态编码

| 任务 | LED编码（板载,LED1,LED2）|
|---|---|
| TASK1 | 1 0 1 |
| TASK2 AB | 1 1 1 |
| TASK2 AD | 0 1 1 |
| TASK3 AB | 1 0 0 |
| TASK3 AD | 0 0 0 |
| TASK4 | 1 1 0 |

### 3.9 蜂鸣器

- 非阻塞设计：置buzzer.on=1，设buzzer.off_time = time + 持续时间
- 每周期检查：if (buzzer.on && time >= buzzer.off_time) buzzer.on = 0
- 不使用HAL_Delay()
- PD2短按：200ms短响；PB11长按：1000ms长响；task_finish：200ms短响

---

## 四、TASK状态机详细参数（Defect.c）

### TASK1（Q1）关键参数
- 速度：FIRST_TURN=30, FLASH=55, CRUISE=38, TURN=23, FINAL_SLOW=20
- 角度：A=80°, D=85°, C=85°, B=85°
- 角度容差：转弯完成8°，找线接管8°
- 路程阈值：STRAIGHT_FLASH=58cm, PRE_TURN_SLOW=70cm(减速到25), FINAL_SLOW=63cm(减速到20)
- 停车：BA边LeftRoad + 里程>80cm → STOP

### TASK2 AB（Q2_AB）关键参数
- 速度：FIRST_TURN=38, FLASH=55, CRUISE=40, TURN=23, UTURN=20, FINAL_SLOW=20
- 掉头前预减速：>70cm降速到20
- 掉头停车条件：|wheel[0].cur_speed|≤4 && |wheel[1].cur_speed|≤4
- 角度：掉头175°，90°转弯76-86°
- 掉头角度限幅放大到93（普通27）
- CB/BC路口使能：79cm

### TASK3 AB（Q3_AB）关键参数
- 速度：FIRST_TURN=36, FLASH=58, CRUISE=43, TURN=26, FINAL_SLOW=20
- CD边：CD_START=30, CD_SLOW=20(14-68cm), CD_FAST=30(>68cm)
- CD路口忽略距离：75cm（屏蔽A4纸横线干扰）
- 路口使能距离：70cm

### 速度曲线策略（所有TASK共用模式）
```
阶段开始 → 稳定前：低速找线(FINAL_SLOW)
      0 ~ STRAIGHT_FLASH_CM：最高速冲刺
      STRAIGHT_FLASH ~ PRE_TURN_SLOW：中速巡航
      > PRE_TURN_SLOW：入弯前减速
```

---

## 五、MaixCam K230通信协议

**物理层**：USART3, 115200bps, 8N1

**命令格式**（与STM32 UART调参协议共用格式）：
```
C <cmd_char> <value> \r\n
```

**TASK3相关**：
- Cl<值> → l1 测量值
- CL<值> → l2 测量值

**TASK4相关**（待实现框架）：
- 触发拍照：timer_it.c中有预留UART指令模板 `maixcam[3] = {0xAA, 0x02, 0xBB}`
- 识别结果回传：形状编号 0=圆形, 1=正方形, 2=三角形, 3=椭圆形

**K230角色**：只负责视觉采集+识别，不参与电机控制。电机控制全部由STM32完成。

---

## 六、PID算法细节

**文件**：`User/Tool/pid.c`

**变速积分**（compute_pid, line 38-46）：
```c
// 误差越大积分越慢，防止积分饱和
k = 1.0f / (1.0f + InteralCoef × |error|)
integral += error × T × k
integral = CONFINE(integral, -integral_max, integral_max)
```

**微分项**：首次计算derivative=0，之后=(error-last_error)/T

**PID输出**：out = kp×error + ki×integral + kd×derivative

**各PID环当前参数总结**：

| PID环 | kp | ki | kd | T | integral_max | InteralCoef |
|---|---|---|---|---|---|---|
| 巡线环(BASIC) | 1.0 | 0.03 | 0 | 20 | 20 | 0 |
| 巡线环(ADV) | 1.0 | 0.03 | 0 | 20 | 20 | 0 |
| 角度环(BASIC) | 1.0 | 0 | 0 | 20 | 20 | 0 |
| 角度环(ADV) | 1.0 | 0 | 0 | 20 | 20 | 0 |
| 左轮速度环 | 8.0 | 0 | 0 | 20 | 100 | 0.50 |
| 右轮速度环 | 8.0 | 0 | 0 | 20 | 100 | 0.50 |

---

## 七、20ms控制周期完整调用链

```
TIM5中断(1ms)
  → time += 1
  → if (time % 20 == 0): update_status(&status)

update_status():
  1. driver_gw_analogue()          → 灰度ADC + 二值化 + diff + 路口检测
  2. get_wheel_speed() × 4         → 编码器读取
  3. get_gyr_raw_data()  → I2C读GY901
  4. status.state.cur_angle = get_gyr_value(gyr_z_yaw)
  5. driver_button(D2), driver_button(B11)  → 按键状态机
  6. update_task(&status)           → 消费request + 任务状态机
  7. switch(motion):
       FIND_LINE  → follow_line()      → 灰度偏差PID → tar_speed
       KEEP_ANGLE → keep_angle()       → 角度偏差PID → tar_speed
       STOP       → tar_speed = 0, stop_cmd = 1
       MOTOR_TEST → tar_speed = cmd_speed
  8. driver_LED() × 3               → 写GPIO
  9. driver_servo() × 2             → 写PWM
  10. driver_BUZZER()               → 超时判断 + 写GPIO
  11. driver_wheel() × 2            → 前馈 + 速度PID → PWM输出
```

---

## 八、TASK三层架构

| 层级 | 字段 | 含义 | 修改者 |
|---|---|---|---|
| 选题层 | task_id | 当前第几问(1-4) | task_select() |
| 选题层 | start_pose | AB/AD发车 | pose_switch |
| 题内流程 | race_phase | 题内阶段 | task_xxx_update() |
| 题内流程 | cross_cnt | 任务确认的有效路口数 | task_xxx_update() |
| 请求位 | start_request | 请求发车 | 按钮/蓝牙 |
| 请求位 | task_select_request | 请求切换任务 | 按钮/蓝牙 |
| 请求位 | pose_switch_request | 请求切换AB/AD | 按钮/蓝牙 |
| 入口 | armed | 入口放行 | update_task() |
| 运行 | task_running | 任务内部确认运行 | task_xxx_update() |
| 运动控制 | motion | STOP/FIND_LINE/KEEP_ANGLE | task_xxx_update() |
| 运动控制 | base_speed | 基础速度 | task_xxx_update() |

**request/armed/task_running三者关系**：
```
start_request = 1 (按钮/蓝牙)
  → update_task() 消费: armed = 1, task_start()
  → task_xxx_update() 确认任务开始: task_running = 1
  → task_finish(): task_running = 0, armed = 0
```
