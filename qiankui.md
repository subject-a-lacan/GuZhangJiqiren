# TASK2 min_pwm 测量说明

这次要测的不是电机空转死区，而是：

```text
小车完整落地、带摆杆负载时，能产生明显平移加速度的最小 PWM
```

这个值后面会作为 `task2_balance_min_pwm`，加在最终控制输出 `u` 上，用来避免控制器算出来的 PWM 太小、车轮虽然转了但没有足够救摆加速度。

## 1. 为什么要测这个

现在的现象是：

```text
P 给了以后，车最后会跑得很快
但摆已经快倒到支架时，车才开始明显加速
而且加速度不够救回摆
```

这说明问题不只是电机“能不能转”，而是：

```text
给某个 PWM 后，小车能不能立刻产生足够大的加速度
```

空转死区只能说明轮子悬空时能不能转，不能说明车落地以后能不能救摆。所以要在完整负载下测。

## 2. 当前测试代码怎么用

当前 `调min_pwm` 分支已经支持串口命令：

```text
C500    两轮同向给 PWM 500
C-500   两轮同向给 PWM -500
L500    只给左轮 PWM 500
R500    只给右轮 PWM 500
Cz      停车
```

注意：现在这些 PWM 会先经过 PPPPPP 分支里的左右轮 PWM 补偿逻辑，再输出到电机。这样测出来的数据和 TASK2 直接 PWM 控制时更一致。

主循环会通过 `huart1` 近似每 8ms 打印一行：

```text
左轮速度,右轮速度\r\n
```

这里的速度就是 8ms 内编码器累计值。

## 3. 要测哪些 PWM

先测两轮同向：

```text
正向：C300, C400, C500, C600, C700, C800
反向：C-300, C-400, C-500, C-600, C-700, C-800
```

每个 PWM 都从静止开始测。

推荐流程：

```text
1. 发 Cz，确认车停住
2. 发 C300
3. 记录刚开始的 10~20 行速度数据
4. 发 Cz
5. 等车停稳
6. 换 C400，重复
```

## 4. 记录什么

记录每个 PWM 下刚启动时的速度序列，比如：

```text
C500:
0,0
1,1
2,2
5,4
8,7
11,10
```

重点看速度有没有连续增长。

可以粗略看加速度：

```text
accel ~= speed_now - speed_last
```

例子：

```text
300:
0,0
1,1
1,1
1,1
2,1

500:
0,0
2,2
5,4
8,7
11,10
```

`300` 只是勉强动，不算有效救摆 PWM。`500` 有连续明显增长，才算有效。

## 5. 怎么判断 min_pwm

判断标准：

```text
连续 3 个控制周期速度同方向增长
并且平均增量 >= 2~3 个编码器脉冲/8ms
```

如果第一个明显满足条件的是 `C500`，那么：

```text
task2_balance_min_pwm = 500
```

如果正向和反向差不多，可以先用一个统一值。

如果差很多，可以分开：

```text
task2_balance_min_pwm_forward
task2_balance_min_pwm_backward
```

第一版建议先用统一值，简单验证有没有效果。

## 6. 为什么不是看轮子会不会转

因为救摆需要的是底盘加速度，不是轮子刚好转起来。

下面这种不算：

```text
轮子转了
但速度几乎不增长
车只是慢慢爬
或者轻微扰动后就不动
```

真正有用的是：

```text
给 PWM 后，编码器速度在连续几个 8ms 周期里明显变大
```

这说明小车确实获得了加速度。

## 7. 测完之后怎么用

后面在 TASK2 稳定阶段，把 PID/LQR 算出的最终输出记作 `u`：

```c
float u = balance_out + position_out;
```

然后统一加 min_pwm：

```c
if (ABS(u) > 1.0f) {
    if (u > 0) {
        u += task2_balance_min_pwm;
    } else {
        u -= task2_balance_min_pwm;
    }
}

task2_set_pwm((int16_t)u);
```

这个补偿加在最终输出 `u` 上，不只加在角度项上。这样以后加入位置项、车速项时也不会打架。
